import * as actionTypes from '../actionTypes';

const INITIAL_STATE = {
  isLoading: false,
  matches: [],
};


export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
